function createDocument(name: string) {}

function createArrayType() {}

// defines what a composite crdt type can do.
const rundown = createDocument('rundown', [{ type: 'row', content: {} }]);

// script - store, how? text vs prosemirror schema vs simplified?

// rundown?
